name: KCC Loan Automation

on:
  workflow_dispatch:   # Manual trigger from GitHub UI — only YOU can trigger this

jobs:
  automate:
    # ── Use self-hosted runner if available (no 6hr limit)
    # ── Change to: runs-on: ubuntu-latest  for GitHub free machine
    runs-on: ubuntu-latest
    timeout-minutes: 360   # 6 hours (GitHub hosted) — no limit on self-hosted

    steps:

    # ── 1. Checkout code ─────────────────────────────────────────────
    - name: Checkout
      uses: actions/checkout@v4

    # ── 2. Install Chrome + virtual display + noVNC ──────────────────
    - name: Install Chrome and display tools
      run: |
        sudo apt-get update -qq
        sudo apt-get install -y wget gnupg2 xvfb x11vnc novnc websockify
        wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
        echo "deb http://dl.google.com/linux/chrome/deb/ stable main" | \
          sudo tee /etc/apt/sources.list.d/google-chrome.list
        sudo apt-get update -qq
        sudo apt-get install -y google-chrome-stable
        google-chrome --version

    # ── 3. Install Python dependencies ───────────────────────────────
    - name: Install Python dependencies
      run: pip install -r requirements.txt

    # ── 4. Download cloudflared (Linux) ──────────────────────────────
    - name: Download cloudflared
      run: |
        wget -q https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 \
          -O cloudflared
        chmod +x cloudflared

    # ── 5. Start virtual display ─────────────────────────────────────
    - name: Start virtual display
      run: |
        Xvfb :99 -screen 0 1920x1080x24 &
        sleep 2

    # ── 6. Start VNC + noVNC ─────────────────────────────────────────
    - name: Start VNC and noVNC
      run: |
        x11vnc -display :99 -forever -nopw -quiet &
        websockify --web /usr/share/novnc 6080 localhost:5900 &
        sleep 2

    # ── 7. Start Dashboard (with password from GitHub Secret) ────────
    - name: Start Dashboard
      env:
        DASHBOARD_PASSWORD: ${{ secrets.DASHBOARD_PASSWORD }}
        SECRET_KEY: ${{ secrets.SECRET_KEY }}
      run: |
        DISPLAY=:99 python src/dashboard.py &
        sleep 5
        echo "Dashboard started with password protection ✅"

    # ── 8. Start Cloudflare tunnels & print URLs ─────────────────────
    - name: Start Cloudflare tunnels and show URLs
      run: |
        ./cloudflared tunnel --url http://localhost:5000 > /tmp/cf_dash.log 2>&1 &
        ./cloudflared tunnel --url http://localhost:6080 > /tmp/cf_vnc.log 2>&1 &

        echo "Waiting for tunnels..."
        sleep 20

        DASHBOARD_URL=$(grep -o 'https://[a-z0-9-]*\.trycloudflare\.com' /tmp/cf_dash.log | head -1)
        VNC_URL=$(grep -o 'https://[a-z0-9-]*\.trycloudflare\.com' /tmp/cf_vnc.log | head -1)

        echo ""
        echo "========================================================"
        echo "  ✅  KCC AUTOMATION IS READY!"
        echo "========================================================"
        echo ""
        echo "  📊  DASHBOARD  →  $DASHBOARD_URL"
        echo "      (Password protected — share with operators)"
        echo ""
        echo "  🖥️   VNC        →  $VNC_URL/vnc.html"
        echo "      (Login to fasalrin.gov.in here)"
        echo ""
        echo "  🔐  PASSWORD    →  (set in GitHub Secrets)"
        echo "  ⚠️   URLs change every run — share fresh URLs each time!"
        echo "========================================================"

    # ── 9. Keep alive ────────────────────────────────────────────────
    - name: Keep alive
      run: |
        echo "Automation running... staying alive"
        sleep 21600
